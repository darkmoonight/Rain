import 'package:flutter/material.dart';

/// Material You widget preview colors aligned with Android `colors.xml`.
abstract final class WidgetThemePreviewColors {
  /// Light widget root background (`values/colors.xml`).
  static const lightBackground = Color(0xFFF0F4F8);

  /// Dark widget root background (`values-night/colors.xml`).
  static const darkBackground = Color(0xFF1A1C1E);

  /// Typical primary text on the light widget surface.
  static const lightText = Color(0xFF1A1C1E);

  /// Typical primary text on the dark widget surface.
  static const darkText = Color(0xFFE2E2E6);
}
